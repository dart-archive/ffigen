name: simple_Example

environment:
  sdk: '>=2.8.1 <3.0.0'

dev_dependencies:
  ffigen: 
    path: '../../'

ffigen:
  output: 'generated_bindings.dart'
  libclang-dylib: '../../tool/wrapped_libclang/libwrapped_clang.so'
  headers:
    - 'headers/example.h'