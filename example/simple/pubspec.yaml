name: simple_Example

environment:
  sdk: '>=2.8.1 <3.0.0'

dev_dependencies:
  ffigen: 
    path: '../../'

ffigen:
  output: 'generated_bindings.dart'
  libclang-dylib-folder: '../../tool/wrapped_libclang'
  headers:
    - 'headers/example.h'