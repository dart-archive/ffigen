name: example_ffi_generation
description: A simple command-line application.
# version: 1.0.0
# homepage: https://www.example.com

environment:
  sdk: '>=2.8.1 <3.0.0'

dev_dependencies:
  ffigen: 
    path: '../'

ffigen:
  libclang_dylib: '../tool/wrapped_libclang/libwrapped_clang.so'
  headers:
    - 'headers/example.h'